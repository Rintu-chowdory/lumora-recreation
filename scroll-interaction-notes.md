# Interactive Scroll Verification

The live preview was exercised from a different section using browser interaction. Activating the Back to Top control returned the viewport to `scrollY: 1` and hid the control. Activating the Services navigation link from the hero moved the viewport to `scrollY: 3013`, with the Services section top at `16px`, confirming the smooth-scroll target resolves correctly.
