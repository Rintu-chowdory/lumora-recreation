/* Lumora Recreation — app shell kept intentionally minimal so the single-page editorial experience owns the viewport. */
import Home from './pages/Home';
import './pages/home.css';

export default function App() {
  return <Home />;
}
